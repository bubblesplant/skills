# 组件命名

- `components/` 下第一层组件的文件名或目录名使用大驼峰命名（PascalCase），例如 `components/UserCard.tsx`、`components/Header/index.tsx`。
- 组件内部子组件的文件名或目录名使用小写，例如 `components/Header/time.tsx`、`components/Header/weather.tsx`。
- 代码中的组件名仍使用大驼峰命名（PascalCase），例如 `Header`、`Time`、`Weather`；内部子组件的小写约定仅适用于文件名和目录名。
