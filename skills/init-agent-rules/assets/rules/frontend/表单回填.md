# 表单回填

- 开启浏览器回填（自动填充）的输入框，必须为 `:autofill`/`:-webkit-autofill`（含 `:hover`、`:focus`、`:active`）状态设置 `transition: background-color 900000000s ease-in-out 0s;`，冻结浏览器注入的填充底色，保持输入框自身底色；该底色是带 `!important` 的 UA 样式，普通背景声明无法覆盖。
- 填充态文字色用 `-webkit-text-fill-color` 指定，并显式设置 `caret-color` 保证光标可见。
